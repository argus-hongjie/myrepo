language: java
sudo: false
jdk: oraclejdk8
script:
 - cd $TRAVIS_BUILD_DIR/myrepo && mvn clean test
 - cd $TRAVIS_BUILD_DIR/usedjar && mvn clean install
 - cd $TRAVIS_BUILD_DIR/depprj && mvn clean test
 
